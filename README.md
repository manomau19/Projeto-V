
  # Sistema de agendamento V

  This is a code bundle for Sistema de agendamento V. The original project is available at https://www.figma.com/design/IbTRwo8WdzVS4e4TekG26i/Sistema-de-agendamento-V.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  